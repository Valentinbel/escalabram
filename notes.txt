
On ne veut pas que les autres changent leur pseudo. Donc je ne veux pas pouvoir le faire non plus.
    Alors on avertit lors du register que ce nom sera notre pseudo visible par tous les users.

==> ACTION en BACK:
    On Supprime le userName du Profile
        Supprimer de l'entity
        Modifier dans le ProfileMapper voir dans TIG comment on fait plusieurs parametres de mapper

==> Action en FRONT:
    linker en Front avec un update du userName du User
    Mettre dans la navBar un Hello username + Avatar

==> Tester avec Postman qu'il y a bien des erreurs avec et sans @Validated
    Dans les Controllers si on ne respecrte pas les @Size...

==> Revalider:
    On a : @PostMapping("/profiles") ==> Pluriel

    et: @PutMapping("/user/{userId}/{userName}") ==> Singulier

    // FILE UPLOAD:
    https://www.bezkoder.com/spring-boot-file-upload/
    * Ce qu'il y a dans le controller: a mettre dans un service, ou dans le profileController
    * Mettre les autres classes:
        * FileUploadExceptionAdvice

    http://stackoverflow.com/questions/71493907/sending-a-multipartfile-from-angular-to-java

     * Un truc pour compresser automatiquement les images avant de les uploader. (depuis le front)